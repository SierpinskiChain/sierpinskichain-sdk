"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractWasmExports = extractWasmExports;
exports.abiFromWasmExports = abiFromWasmExports;
exports.generateTypeScriptBindings = generateTypeScriptBindings;
function toUint8Array(bytes) {
    if (bytes instanceof Uint8Array)
        return bytes;
    if (bytes instanceof ArrayBuffer)
        return new Uint8Array(bytes);
    return new Uint8Array(bytes.buffer, bytes.byteOffset, bytes.byteLength);
}
function inferMode(name) {
    const lower = name.toLowerCase();
    if (lower.startsWith("query_") ||
        lower.startsWith("get_") ||
        lower.startsWith("read_") ||
        lower.startsWith("view_")) {
        return "query";
    }
    return "call";
}
function extractWasmExports(bytes) {
    // Copy to a plain ArrayBuffer-backed view for strict TS compatibility.
    const mod = new WebAssembly.Module(Uint8Array.from(toUint8Array(bytes)));
    return WebAssembly.Module.exports(mod).map((entry) => ({
        name: entry.name,
        kind: entry.kind,
    }));
}
function abiFromWasmExports(bytes) {
    const methods = extractWasmExports(bytes)
        .filter((entry) => entry.kind === "function")
        .map((entry) => ({ name: entry.name, mode: inferMode(entry.name) }));
    return { methods };
}
function generateTypeScriptBindings(abi, options) {
    const clientVar = options.clientVarName ?? "client";
    const lines = [];
    lines.push("// Auto-generated by @sierpinskichain/sdk/contract");
    lines.push("import type { ContractClient } from \"@sierpinskichain/sdk/contract\";");
    lines.push("");
    lines.push(`export function create${options.contract}Bindings(${clientVar}: ContractClient) {`);
    lines.push("  return {");
    for (const method of abi.methods) {
        if (method.mode === "query") {
            lines.push(`    async ${method.name}(key: string) { return ${clientVar}.query({ contract: "${options.contract}", key }); },`);
        }
        else {
            lines.push(`    async ${method.name}(key: string, value: string) { return ${clientVar}.call({ contract: "${options.contract}", key, value }); },`);
        }
    }
    lines.push("  };");
    lines.push("}");
    return lines.join("\n");
}
//# sourceMappingURL=contract-abi.js.map